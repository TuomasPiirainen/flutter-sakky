import 'package:flutter/material.dart';
import 'package:vko_kaksi/custom_container.dart';

// Dart alkaa
void main() {
  runApp(
// shift + alt + f
    const MaterialApp(
      home: Scaffold(
        body: CustomContainer(),
      ),
    ),
  );
}
